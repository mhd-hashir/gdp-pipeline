# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Env
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Environment Variables
.env

# IDEs
.vscode/
.idea/

# Data
# We usually don't commit large data, but maybe the user wants the CSV committed?
# I'll keep the CSV in for now as it's small output, but exclude raw if any.
# For now, no specific data exclusion as the CSV is small (26 rows).
